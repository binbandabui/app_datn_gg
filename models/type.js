// const mongoose = require("mongoose");

// // Define the schema for "Type"
// const typeSchema = mongoose.Schema({
//   type: {
//     type: String, // The "type" field is a string
//     required: true, // It is required (must be provided)
//     default: "", // If not provided, defaults to an empty string
//   },
// });

// // Create a model called "Type" using the schema
// const Type = mongoose.model("Type", typeSchema);

// // Export the model so it can be used in other files
// module.exports = Type;
